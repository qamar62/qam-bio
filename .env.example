# Password required by /admin and the projects API (x-admin-password header)
ADMIN_PASSWORD=

# Vercel Blob token — stores projects.json and uploaded project images.
# Auto-injected on Vercel once a Blob store is linked; set manually elsewhere.
BLOB_READ_WRITE_TOKEN=
